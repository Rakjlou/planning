{
  "name": "retroplanning-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "seed": "node seed.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "ejs": "^5.0.1",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "playwright": "^1.58.2"
  }
}
